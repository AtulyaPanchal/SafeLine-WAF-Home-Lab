# /etc/apache2/ports.conf
# Change Apache's listening port from 80 to 8080 so SafeLine WAF
# can own port 443/80 as the public-facing reverse proxy.
#
# BEFORE:
# Listen 80
#
# AFTER:
Listen 8080

<IfModule ssl_module>
	Listen 443
</IfModule>

<IfModule mod_gnutls.c>
	Listen 443
</IfModule>
